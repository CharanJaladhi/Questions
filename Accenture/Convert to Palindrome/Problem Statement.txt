Given a function,  ConvertToPalindrome(char* str) The function accepts a string str, implement the function to find and return the minimum characters required to append at the end of str to make it a palindrome.

Input :
The string input of length 100 is given.

Constraints :
The string will only contain lowercase English Alphabets

Output :
If string is already a palindrome then return NULL · You have to find the minimum characters required to append at the end of the string to make it a palindrome.

Example 1
Input
abcdc
Output
ba	

Example 2
Input
malayalam
Output
NULL